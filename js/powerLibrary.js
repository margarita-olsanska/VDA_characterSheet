// a shared, growing catalog of discipline powers the player defines through
// the app itself - independent of any single character, so a power defined
// once (discipline + level + name + description) can be reused on any
// character's discipline cards from then on
export const powerLibrary = {}
